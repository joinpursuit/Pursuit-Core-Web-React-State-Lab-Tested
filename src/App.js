import React from "react";

import "./App.css";

class App extends React.Component {
  render() {
    return (
      
      <div>
        <h1>Current Score: {score}</h1>
        <button onClick={}> +{value}</button>
        <button onClick={}> Pay 10 points to change from +1 to +2</button>
      {/* <Score/> */}
      </div>
    );
  }
}

export default App;
